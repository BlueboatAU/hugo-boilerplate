# Hugo Boilerplate by Studio Blueboat

A boilerplate for hugo websites using gulp, babel and sass.

run directly after download:
npm install

develop with:
npm run dev

build with:
npm run build

deploy hugo/public
